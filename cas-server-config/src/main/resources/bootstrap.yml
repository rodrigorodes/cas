
spring:
  profiles:
    active: git
---
spring:
  profiles: native
  cloud:
    config:
      server:
        native:
          search-locations:
            file://PATH_TO_MICROSERVICES_PROJECT/ms-config-properties/{application}/{profile},
            file://PATH_TO_MICROSERVICES_PROJECT/ms-config-properties/global/{profile}
---
spring:
  profiles: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rodrigorodes/cas/
          search-paths:
            - "cas-repo-config/{application}/{profile}"
         
            
            
            
            

spring.application.name=cas
spring.profiles.active=native,default,security
spring.cloud.config.server.git.uri=https://github.com/rodrigorodes/cas/
spring.cloud.config.server.git.uri.search-paths=cas-repo-config/{application}/{profile}

#encrypt.key-store.location=file:///etc/cas/casconfigserver.jks
#encrypt.key-store.password=changeit
#encrypt.key-store.alias=cas